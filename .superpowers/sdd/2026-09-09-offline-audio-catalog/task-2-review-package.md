# Review package — Task 2

This project has no Git history, so all files below are new for this task. Review the current versions of:

- `app.js`
- `tests/app.test.mjs`

Required behavior: export pure `textFor`, `formatDuration`, and `filterEntries` helpers. `textFor` selects Polish or Ukrainian description. Search must case-insensitively cover title, Ukrainian transcript, and both descriptions. Duration 65 must format as `1:05`. DOM work is intentionally excluded. The test evidence is in `task-2-report.md`.
